// Fuction to return the area of a triangle
double calculateArea(double height, double base) {
  double triangleArea = (0.5 * base * height);
  return (double.parse(triangleArea.toStringAsFixed(2)));
}